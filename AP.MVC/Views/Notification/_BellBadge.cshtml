@model int
<a class="fu-bell-btn" href="@Url.Action("Index", "Notification")" title="Notificaciones">
    <span class="fu-bell-icon">&#x1F514;</span>
    @if (Model > 0)
    {
        <span class="fu-bell-count">@(Model > 99 ? "99+" : Model.ToString())</span>
    }
</a>
